test:
  single:
    - securities:
        - ticker: TQQQ
          underlying_ticker: ^NDX
          daily_return_multiplier: 3
          expense_ratio: 0.0082
          weight: 1.0
      strategy:
        # name: SimpleMovingAverageRebalance
        # target: ^NDX
        # primary: TQQQ
        # alternate: QQQ
        # dxy_adjusted: false
        name: ScheduledRebalance
        schedule:
          mode: static
          format: DAYS
          value: 0
      allocation:
        mode: static
        schedule:
          format: DAYS
          value: 7
        initial: 10000
        yearly: 5000
        weights:
          TQQQ: 1.0
      dates:
        start: "1986-05-01"
        #end: "2002-10-09"
      performance:
        benchmark: ^NDX
        risk_free_ticker: ^IRX
  #     trace: false
  # simulate:
  #   - securities:
  #       - ticker: TQQQ
  #         weight: 1.0
  #       - ticker: QQQ
  #         weight: 0.0
  #     strategy:
  #       name: ScheduledRebalance
  #       schedule:
  #         mode: static
  #         format: DAYS
  #         value: 7
  #     allocation:
  #       mode: dynamic
  #       increment: 4
  #       initial: 10000
  #       yearly: 6000
  #     weights:
  #       mode: static
  #     dates:
  #       start: "2026-01-30"
  #       # end: "2026-01-30"
  #     trace: false
